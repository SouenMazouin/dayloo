import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontSize: 25,
    textAlign: 'center',
    paddingBottom: 5,
  },
  titleContainer: {
    alignItems: 'center',
    marginTop: 0,
  },
  subtitle: {
    marginBottom: 5,
  },
});

export default styles;
