import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  blocksContainer: {
    paddingTop: 8,
    paddingLeft: 1,
    paddingRight: 15,
  },
  blockExample: {
    fontWeight: 'bold',
  },
});

export default styles;
