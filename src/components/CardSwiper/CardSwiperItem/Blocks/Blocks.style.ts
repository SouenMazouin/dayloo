import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  blocksContainer: {
    marginTop: 10,
    paddingTop: 15,
    paddingLeft: 1,
    paddingRight: 5,
  },
});

export default styles;
