import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  titleContainer: {
    alignItems: 'center',
    marginTop: 0,
    paddingLeft: 10,
    paddingRight: 10,
  },
  subtitle: {
    fontStyle: 'italic',
    fontSize: 15,
  },
});

export default styles;
